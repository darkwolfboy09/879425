function setup(){
    canvas = createCanvas(400,400);
    canvas.position(550,200);
    video = createCapture(VIDEO);
    video.size(400,400);
    video.position(550,200)
  }
  function draw(){
  
  }
  function preload(){
  
  }